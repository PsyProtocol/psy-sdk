#[derive(StorageRef)]
#[derive(Storage)]
pub struct Person {
    pub age: Felt,
    pub height: Felt,
    pub year_of_birth: Felt,
}

#[contract]
#[derive(StorageRef)]
#[derive(Storage)]
pub struct SimpleContract {
    pub a: Felt,
    pub b: Person,
    pub c: Felt,
}

// input:
// output:
fn main() -> Felt {
    return 1;
}
