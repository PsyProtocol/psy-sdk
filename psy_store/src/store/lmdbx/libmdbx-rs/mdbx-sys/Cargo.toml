[package]
name = "reth-mdbx-sys"
description = "Raw bindings for libmdbx"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
license = "Apache-2.0"

[build-dependencies]
cc = { workspace = true }
bindgen = { workspace = true, features = ["runtime"] }