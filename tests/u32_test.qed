
fn main(a: u32, b: u32) -> Felt {
    let k: u32 = (a + 2u32) * (2u32 * 2u32) * b - 3u32 * (a + b);
    let mut z: u32 = k + a;
    assert_eq(z, 35u32, "z == 35");

    z = z / a;
    assert_eq(z, 17u32, "z == 17");

    let zz: u32 = if z > 1u32 {
        1u32
    } else {
        0u32
    };

    let bb: bool = zz as bool;
    return bb as Felt;
}