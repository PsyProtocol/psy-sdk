AWSTemplateFormatVersion: '2010-09-09'
Description: 'QED Protocol AWS Deployment - All ECS Services with Auto Scaling (Simplified Network)'

Parameters:
  ProjectName:
    Type: String
    Default: qed-protocol
    Description: Name of the project

  Environment:
    Type: String
    Default: production
    Description: Environment name

  ContainerImage:
    Type: String
    Default: "466080142804.dkr.ecr.ap-northeast-1.amazonaws.com/qed-protocol-rollup:latest"
    Description: Docker image for QED services

  LogLevel:
    Type: String
    Default: qed_user_cli=debug,qed_dev_cli=debug,qed_rollup_cli=debug,qed_node=debug,qed_common_circuit=debug,qed_rollup_circuit=debug,qed_prover=debug,qed_data=debug,plonky2=error
    Description: Log level configuration

  MinEdgeCount:
    Type: Number
    Default: 1
    Description: Minimum number of edge instances

  MaxEdgeCount:
    Type: Number
    Default: 3
    Description: Maximum number of edge instances

  MinWorkerCount:
    Type: Number
    Default: 1
    Description: Minimum number of worker instances

  MaxWorkerCount:
    Type: Number
    Default: 3
    Description: Maximum number of worker instances

  TargetCPUUtilization:
    Type: Number
    Default: 70
    Description: Target CPU utilization percentage for auto scaling

  CoordinatorRedisEndpoint:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /qed-protocol/redis/coordinator/endpoint
    Description: SSM parameter for Coordinator Redis endpoint

  Realm0RedisEndpoint:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /qed-protocol/redis/realm0/endpoint
    Description: SSM parameter for Realm0 Redis endpoint

  Realm1RedisEndpoint:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /qed-protocol/redis/realm1/endpoint
    Description: SSM parameter for Realm1 Redis endpoint

  CoordinatorScyllaDBEndpoint:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /qed-protocol/scylladb/coordinator-endpoint
    Description: SSM parameter for Coordinator ScyllaDB endpoint

  Realm0ScyllaDBEndpoint:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /qed-protocol/scylladb/realm0-endpoint
    Description: SSM parameter for Realm0 ScyllaDB endpoint

  Realm1ScyllaDBEndpoint:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /qed-protocol/scylladb/realm1-endpoint
    Description: SSM parameter for Realm1 ScyllaDB endpoint

Resources:
  # Task Definitions
  CoordinatorProcessorTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-coordinator-processor
      NetworkMode: bridge
      PidMode: host
      RequiresCompatibilities:
        - EC2
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: coordinator-processor
          Image: !Ref ContainerImage
          Memory: 8192
          MemoryReservation: 6144
          Cpu: 2048
          LinuxParameters:
            InitProcessEnabled: true
          Command:
            - coordinator-processor
            - !Sub --redis-uri=redis://${CoordinatorRedisEndpoint}
            - !Sub --scylla-uri=${CoordinatorScyllaDBEndpoint}
            - --scylla-keyspace=qed_coordinator
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: coordinator-processor
          Essential: true

  CoordinatorWorkerTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-coordinator-worker
      NetworkMode: bridge
      RequiresCompatibilities:
        - EC2
      Cpu: '8192'
      Memory: '32'
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: coordinator-worker
          Image: !Ref ContainerImage
          Command:
            - coordinator-worker
            - !Sub --redis-uri=redis://${CoordinatorRedisEndpoint}
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
            - Name: WORKER_THREADS
              Value: "8"
            - Name: WORKER_PERFORMANCE_MODE
              Value: "high"
          Memory: 12288
          MemoryReservation: 8192
          Cpu: 4096
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: coordinator-worker
          Essential: true

  CoordinatorEdgeTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-coordinator-edge
      NetworkMode: bridge
      PidMode: host
      RequiresCompatibilities:
        - EC2
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: coordinator-edge
          Image: !Ref ContainerImage
          Memory: 8192
          MemoryReservation: 6144
          Cpu: 2048
          LinuxParameters:
            InitProcessEnabled: true
          Command:
            - coordinator-edge
            - --listen-addr=0.0.0.0:8545
            - !Sub --redis-uri=redis://${CoordinatorRedisEndpoint}
            - !Sub --scylla-uri=${CoordinatorScyllaDBEndpoint}
            - --scylla-keyspace=qed_coordinator
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
          PortMappings:
            - ContainerPort: 8545
              HostPort: 0
              Protocol: tcp
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: coordinator-edge
          Essential: true

  Realm0ProcessorTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-realm0-processor
      NetworkMode: bridge
      PidMode: host
      RequiresCompatibilities:
        - EC2
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: realm0-processor
          Image: !Ref ContainerImage
          Memory: 8192
          MemoryReservation: 6144
          Cpu: 2048
          LinuxParameters:
            InitProcessEnabled: true
          Command:
            - realm-processor
            - !Sub --redis-uri=redis://${Realm0RedisEndpoint}
            - !Sub --scylla-uri=${Realm0ScyllaDBEndpoint}
            - --scylla-keyspace=qed_realm_0
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: realm0-processor
          Essential: true

  Realm0WorkerTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-realm0-worker
      NetworkMode: bridge
      RequiresCompatibilities:
        - EC2
      Cpu: '8192'
      Memory: '32'
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: realm0-worker
          Image: !Ref ContainerImage
          Command:
            - realm-worker
            - !Sub --redis-uri=redis://${Realm0RedisEndpoint}
            - --worker-queue-suffix=rwq0
            - --notifications-queue-suffix=rnq0
            - --proof-store-key-suffix=RP0
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
            - Name: WORKER_THREADS
              Value: "8"
            - Name: WORKER_PERFORMANCE_MODE
              Value: "high"
          Memory: 12288
          MemoryReservation: 8192
          Cpu: 4096
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: realm0-worker
          Essential: true

  Realm0EdgeTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-realm0-edge
      NetworkMode: bridge
      PidMode: host
      RequiresCompatibilities:
        - EC2
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: realm0-edge
          Image: !Ref ContainerImage
          Memory: 8192
          MemoryReservation: 6144
          Cpu: 2048
          LinuxParameters:
            InitProcessEnabled: true
          Command:
            - realm-edge
            - --listen-addr=0.0.0.0:8546
            - !Sub --redis-uri=redis://${Realm0RedisEndpoint}
            - !Sub --scylla-uri=${Realm0ScyllaDBEndpoint}
            - --scylla-keyspace=qed_realm_0
            - !Sub
              - --coordinator-addr=http://${ALBDNS}:8545
              - ALBDNS:
                  Fn::ImportValue: !Sub ${ProjectName}-alb-dns
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
          PortMappings:
            - ContainerPort: 8546
              HostPort: 0
              Protocol: tcp
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: realm0-edge
          Essential: true

  Realm1ProcessorTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-realm1-processor
      NetworkMode: bridge
      PidMode: host
      RequiresCompatibilities:
        - EC2
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: realm1-processor
          Image: !Ref ContainerImage
          Memory: 8192
          MemoryReservation: 6144
          Cpu: 2048
          LinuxParameters:
            InitProcessEnabled: true
          Command:
            - realm-processor
            - --node-id=2
            - --realm-id=1
            - --worker-queue-suffix=rwq1
            - --notifications-queue-suffix=rnq1
            - --proof-store-key-suffix=RP1
            - !Sub --redis-uri=redis://${Realm1RedisEndpoint}
            - !Sub --scylla-uri=${Realm1ScyllaDBEndpoint}
            - --scylla-keyspace=qed_realm_1
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: realm1-processor
          Essential: true

  Realm1WorkerTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-realm1-worker
      NetworkMode: bridge
      RequiresCompatibilities:
        - EC2
      Cpu: '8192'
      Memory: '32'
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: realm1-worker
          Image: !Ref ContainerImage
          Command:
            - realm-worker
            - !Sub --redis-uri=redis://${Realm1RedisEndpoint}
            - --worker-queue-suffix=rwq1
            - --notifications-queue-suffix=rnq1
            - --proof-store-key-suffix=RP1
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
            - Name: WORKER_THREADS
              Value: "8"
            - Name: WORKER_PERFORMANCE_MODE
              Value: "high"
          Memory: 12288
          MemoryReservation: 8192
          Cpu: 4096
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: realm1-worker
          Essential: true

  Realm1EdgeTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${ProjectName}-realm1-edge
      NetworkMode: bridge
      PidMode: host
      RequiresCompatibilities:
        - EC2
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task-execution
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ProjectName}-ecs-task
      ContainerDefinitions:
        - Name: realm1-edge
          Image: !Ref ContainerImage
          Memory: 8192
          MemoryReservation: 6144
          Cpu: 2048
          LinuxParameters:
            InitProcessEnabled: true
          Command:
            - realm-edge
            - --listen-addr=0.0.0.0:8547
            - --node-id=2
            - --realm-id=1
            - --worker-queue-suffix=rwq1
            - --notifications-queue-suffix=rnq1
            - --proof-store-key-suffix=RP1
            - !Sub --redis-uri=redis://${Realm1RedisEndpoint}
            - !Sub --scylla-uri=${Realm1ScyllaDBEndpoint}
            - --scylla-keyspace=qed_realm_1
            - !Sub
              - --coordinator-addr=http://${ALBDNS}:8545
              - ALBDNS:
                  Fn::ImportValue: !Sub ${ProjectName}-alb-dns
          Environment:
            - Name: RUST_LOG
              Value: !Ref LogLevel
            - Name: JWT_SECRET
              Value: "6738d20a5504291acc9a5c3d3ac676bc9f12af02bc14f511ea7f68352d46165f"
          PortMappings:
            - ContainerPort: 8547
              HostPort: 0
              Protocol: tcp
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Sub /ecs/${ProjectName}
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: realm1-edge
          Essential: true

  # Target Groups for Load Balancer
  CoordinatorEdgeTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub ${ProjectName}-coord-edge-tg
      Port: 8545
      Protocol: HTTP
      VpcId:
        Fn::ImportValue: !Sub ${ProjectName}-vpc-id
      TargetType: instance
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: /
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 10
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 10
      Matcher:
        HttpCode: "200,404,405"

  Realm0EdgeTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub ${ProjectName}-realm0-edge-tg
      Port: 8546
      Protocol: HTTP
      VpcId:
        Fn::ImportValue: !Sub ${ProjectName}-vpc-id
      TargetType: instance
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: /
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 10
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 10
      Matcher:
        HttpCode: "200,404,405"

  Realm1EdgeTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub ${ProjectName}-realm1-edge-tg
      Port: 8547
      Protocol: HTTP
      VpcId:
        Fn::ImportValue: !Sub ${ProjectName}-vpc-id
      TargetType: instance
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: /
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 10
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 10
      Matcher:
        HttpCode: "200,404,405"

  # ALB Listeners
  CoordinatorALBListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn:
        Fn::ImportValue: !Sub ${ProjectName}-alb-arn
      Port: 8545
      Protocol: HTTP
      DefaultActions:
        - Type: forward
          TargetGroupArn: !Ref CoordinatorEdgeTargetGroup

  Realm0ALBListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn:
        Fn::ImportValue: !Sub ${ProjectName}-alb-arn
      Port: 8546
      Protocol: HTTP
      DefaultActions:
        - Type: forward
          TargetGroupArn: !Ref Realm0EdgeTargetGroup

  Realm1ALBListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn:
        Fn::ImportValue: !Sub ${ProjectName}-alb-arn
      Port: 8547
      Protocol: HTTP
      DefaultActions:
        - Type: forward
          TargetGroupArn: !Ref Realm1EdgeTargetGroup

  # ECS Services
  CoordinatorProcessorService:
    Type: AWS::ECS::Service
    Properties:
      ServiceName: !Sub ${ProjectName}-coordinator-processor
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref CoordinatorProcessorTaskDefinition
      LaunchType: EC2
      DesiredCount: 1
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:realm == coordinator'

  CoordinatorWorkerService:
    Type: AWS::ECS::Service
    DependsOn: CoordinatorProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-coordinator-worker
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref CoordinatorWorkerTaskDefinition
      LaunchType: EC2
      DesiredCount: !Ref MinWorkerCount
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:ecs.instance-type =~ c6i.*'

  CoordinatorEdgeService:
    Type: AWS::ECS::Service
    DependsOn: CoordinatorProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-coordinator-edge
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref CoordinatorEdgeTaskDefinition
      LaunchType: EC2
      DesiredCount: !Ref MinEdgeCount
      LoadBalancers:
        - ContainerName: coordinator-edge
          ContainerPort: 8545
          TargetGroupArn: !Ref CoordinatorEdgeTargetGroup
      HealthCheckGracePeriodSeconds: 60
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:realm == coordinator'

  Realm0ProcessorService:
    Type: AWS::ECS::Service
    DependsOn: CoordinatorProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-realm0-processor
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref Realm0ProcessorTaskDefinition
      LaunchType: EC2
      DesiredCount: 1
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:realm == realm0'

  Realm0WorkerService:
    Type: AWS::ECS::Service
    DependsOn: Realm0ProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-realm0-worker
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref Realm0WorkerTaskDefinition
      LaunchType: EC2
      DesiredCount: !Ref MinWorkerCount
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:ecs.instance-type =~ c6i.*'

  Realm0EdgeService:
    Type: AWS::ECS::Service
    DependsOn: Realm0ProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-realm0-edge
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref Realm0EdgeTaskDefinition
      LaunchType: EC2
      DesiredCount: !Ref MinEdgeCount
      LoadBalancers:
        - ContainerName: realm0-edge
          ContainerPort: 8546
          TargetGroupArn: !Ref Realm0EdgeTargetGroup
      HealthCheckGracePeriodSeconds: 60
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:realm == realm0'

  Realm1ProcessorService:
    Type: AWS::ECS::Service
    DependsOn: CoordinatorProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-realm1-processor
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref Realm1ProcessorTaskDefinition
      LaunchType: EC2
      DesiredCount: 1
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:realm == realm1'

  Realm1WorkerService:
    Type: AWS::ECS::Service
    DependsOn: Realm1ProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-realm1-worker
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref Realm1WorkerTaskDefinition
      LaunchType: EC2
      DesiredCount: !Ref MinWorkerCount
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:ecs.instance-type =~ c6i.*'

  Realm1EdgeService:
    Type: AWS::ECS::Service
    DependsOn: Realm1ProcessorService
    Properties:
      ServiceName: !Sub ${ProjectName}-realm1-edge
      Cluster:
        Fn::ImportValue: !Sub ${ProjectName}-cluster-name
      TaskDefinition: !Ref Realm1EdgeTaskDefinition
      LaunchType: EC2
      DesiredCount: !Ref MinEdgeCount
      LoadBalancers:
        - ContainerName: realm1-edge
          ContainerPort: 8547
          TargetGroupArn: !Ref Realm1EdgeTargetGroup
      HealthCheckGracePeriodSeconds: 60
      PlacementConstraints:
        - Type: memberOf
          Expression: 'attribute:realm == realm1'

  # Auto Scaling for Worker Services
  CoordinatorWorkerScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    DependsOn: CoordinatorWorkerService
    Properties:
      MaxCapacity: !Ref MaxWorkerCount
      MinCapacity: !Ref MinWorkerCount
      ResourceId: !Sub service/${ProjectName}-cluster/${ProjectName}-coordinator-worker
      RoleARN: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  CoordinatorWorkerCPUScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${ProjectName}-coordinator-worker-cpu-scaling
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref CoordinatorWorkerScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: !Ref TargetCPUUtilization
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  Realm0WorkerScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    DependsOn: Realm0WorkerService
    Properties:
      MaxCapacity: !Ref MaxWorkerCount
      MinCapacity: !Ref MinWorkerCount
      ResourceId: !Sub service/${ProjectName}-cluster/${ProjectName}-realm0-worker
      RoleARN: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  Realm0WorkerCPUScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${ProjectName}-realm0-worker-cpu-scaling
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref Realm0WorkerScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: !Ref TargetCPUUtilization
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  Realm1WorkerScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    DependsOn: Realm1WorkerService
    Properties:
      MaxCapacity: !Ref MaxWorkerCount
      MinCapacity: !Ref MinWorkerCount
      ResourceId: !Sub service/${ProjectName}-cluster/${ProjectName}-realm1-worker
      RoleARN: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  Realm1WorkerCPUScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${ProjectName}-realm1-worker-cpu-scaling
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref Realm1WorkerScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: !Ref TargetCPUUtilization
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  # Auto Scaling for Edge Services
  CoordinatorEdgeScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    DependsOn: CoordinatorEdgeService
    Properties:
      MaxCapacity: !Ref MaxEdgeCount
      MinCapacity: !Ref MinEdgeCount
      ResourceId: !Sub service/${ProjectName}-cluster/${ProjectName}-coordinator-edge
      RoleARN: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  CoordinatorEdgeCPUScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${ProjectName}-coordinator-edge-cpu-scaling
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref CoordinatorEdgeScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: !Ref TargetCPUUtilization
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  Realm0EdgeScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    DependsOn: Realm0EdgeService
    Properties:
      MaxCapacity: !Ref MaxEdgeCount
      MinCapacity: !Ref MinEdgeCount
      ResourceId: !Sub service/${ProjectName}-cluster/${ProjectName}-realm0-edge
      RoleARN: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  Realm0EdgeCPUScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${ProjectName}-realm0-edge-cpu-scaling
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref Realm0EdgeScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: !Ref TargetCPUUtilization
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  Realm1EdgeScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    DependsOn: Realm1EdgeService
    Properties:
      MaxCapacity: !Ref MaxEdgeCount
      MinCapacity: !Ref MinEdgeCount
      ResourceId: !Sub service/${ProjectName}-cluster/${ProjectName}-realm1-edge
      RoleARN: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  Realm1EdgeCPUScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${ProjectName}-realm1-edge-cpu-scaling
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref Realm1EdgeScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: !Ref TargetCPUUtilization
        ScaleInCooldown: 300
        ScaleOutCooldown: 60

  # CloudWatch Alarms for monitoring
  CoordinatorEdgeHighCPUAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub ${ProjectName}-coordinator-edge-high-cpu
      AlarmDescription: Triggers when coordinator edge service CPU is too high
      MetricName: CPUUtilization
      Namespace: AWS/ECS
      Statistic: Average
      Period: 300
      EvaluationPeriods: 2
      Threshold: 80
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: ServiceName
          Value: !Sub ${ProjectName}-coordinator-edge
        - Name: ClusterName
          Value: !Sub ${ProjectName}-cluster

  Realm0EdgeHighCPUAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub ${ProjectName}-realm0-edge-high-cpu
      AlarmDescription: Triggers when realm 0 edge service CPU is too high
      MetricName: CPUUtilization
      Namespace: AWS/ECS
      Statistic: Average
      Period: 300
      EvaluationPeriods: 2
      Threshold: 80
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: ServiceName
          Value: !Sub ${ProjectName}-realm0-edge
        - Name: ClusterName
          Value: !Sub ${ProjectName}-cluster

  Realm1EdgeHighCPUAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub ${ProjectName}-realm1-edge-high-cpu
      AlarmDescription: Triggers when realm 1 edge service CPU is too high
      MetricName: CPUUtilization
      Namespace: AWS/ECS
      Statistic: Average
      Period: 300
      EvaluationPeriods: 2
      Threshold: 80
      ComparisonOperator: GreaterThanThreshold
      Dimensions:
        - Name: ServiceName
          Value: !Sub ${ProjectName}-realm1-edge
        - Name: ClusterName
          Value: !Sub ${ProjectName}-cluster

Outputs:
  CoordinatorEndpoint:
    Description: Coordinator public endpoint
    Value: !Sub
      - http://${ALBDNS}:8545
      - ALBDNS:
          Fn::ImportValue: !Sub ${ProjectName}-alb-dns
    Export:
      Name: !Sub ${ProjectName}-coordinator-endpoint

  Realm0Endpoint:
    Description: Realm 0 public endpoint
    Value: !Sub
      - http://${ALBDNS}:8546
      - ALBDNS:
          Fn::ImportValue: !Sub ${ProjectName}-alb-dns
    Export:
      Name: !Sub ${ProjectName}-realm0-endpoint

  Realm1Endpoint:
    Description: Realm 1 public endpoint
    Value: !Sub
      - http://${ALBDNS}:8547
      - ALBDNS:
          Fn::ImportValue: !Sub ${ProjectName}-alb-dns
    Export:
      Name: !Sub ${ProjectName}-realm1-endpoint

  CoordinatorEdgeTaskDefinition:
    Description: Coordinator Edge Task Definition ARN
    Value: !Ref CoordinatorEdgeTaskDefinition
    Export:
      Name: !Sub ${ProjectName}-coordinator-edge-task-def

  Realm0EdgeTaskDefinition:
    Description: Realm 0 Edge Task Definition ARN
    Value: !Ref Realm0EdgeTaskDefinition
    Export:
      Name: !Sub ${ProjectName}-realm0-edge-task-def

  Realm1EdgeTaskDefinition:
    Description: Realm 1 Edge Task Definition ARN
    Value: !Ref Realm1EdgeTaskDefinition
    Export:
      Name: !Sub ${ProjectName}-realm1-edge-task-def
