{
  "name": "@qstudio/core",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@qstudio/qsvg": "workspace:*",
    "@svgdotjs/svg.js": "^3.2.0",
    "@svgdotjs/svg.panzoom.js": "^2.1.2",
    "uuid": "~10.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "rimraf": "~5.0.0",
    "typescript": "~4.9.0"
  }
}
